version: '3.9'

services:
  llm-explorer:
    build: .
    image: llm-explorer:latest
    container_name: llm-explorer
    ports:
      - "3500:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
